% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getSummary.R
\name{getSummary}
\alias{getSummary}
\title{Run PALM summary statistics for genotype inputs}
\usage{
getSummary(
  genoFile,
  NULLObjPrefix,
  SummaryPrefix,
  vcfField = NULL,
  chrom = NULL,
  featureColList = NULL,
  minMAF = 0.05,
  minMAC = 5,
  maxMissing = 0.15,
  impute_method = "best_guess",
  SnpInfoFile = NULL,
  correct = NULL,
  useCluster = FALSE,
  clusterFile = NULL
)
}
\arguments{
\item{genoFile}{Path to genotype input. If the path ends with
`.vcf`, `.vcf.gz`, or `.vcf.bgz`, VCF input is assumed. Otherwise it is
treated as a PLINK prefix without extension.}

\item{NULLObjPrefix}{Prefix for the fitted null model generated by Step1.
The function reads `<NULLObjPrefix>.rda`.}

\item{SummaryPrefix}{Base prefix for Step2 summary result files. The
function normalizes a trailing underscore when present, then writes one file per feature as
`<SummaryPrefix>_chr<chrom>_<feature>.txt` when `chrom` is set, or
`<SummaryPrefix>_allchr_<feature>.txt` when `chrom` is `NULL`.}

\item{vcfField}{Optional VCF FORMAT field override. By default the reader
auto-detects and prefers `"DS"` when present, otherwise falls back to
`"GT"`. Supported explicit values are `"DS"` and `"GT"`.}

\item{chrom}{Optional chromosome filter (numeric or string like `"chr1"`).
Use `NULL` to keep all chromosomes.}

\item{featureColList}{Optional feature IDs to keep from the Step1 null model.
Accepts either a character vector or a single comma-separated string.
Use `NULL` (default) to keep all modeled features.}

\item{minMAF}{Optional minimum minor allele frequency threshold used to
filter SNPs before running `PALM::palm.get.summary()`. Defaults to `0.05`.
Use `0` to disable MAF filtering.}

\item{minMAC}{Optional minimum minor allele count threshold used to filter
SNPs before running `PALM::palm.get.summary()`. Defaults to `5`. Use `0`
to disable MAC filtering.}

\item{maxMissing}{Maximum per-SNP missing genotype rate. SNPs above this
threshold are removed before imputation and association testing. Defaults
to `0.15`. Use `1` to disable missingness filtering.}

\item{impute_method}{Missing genotype imputation method for SNPs that pass
`maxMissing`. Supported values are `"best_guess"` (most frequent observed
genotype, with mean dosage tie-break), `"mean"` (mean observed dosage), and
`"minor"` (homozygous minor-allele genotype inferred from observed allele
frequency). Use `"false"` to skip imputation. Defaults to `"best_guess"`.}

\item{SnpInfoFile}{Optional output path for SNP sample count and allele
frequency computed from the Step2 genotype matrix after NULL-model sample
alignment, optional chromosome subsetting, missingness filtering,
imputation, and minMAF/minMAC filtering. This describes the final SNP set
used for Step2.1 association testing; the `MissingRate` column is computed
after missingness filtering and before imputation. Use `NULL` (default) to
skip writing this file.}

\item{correct}{Passed to `PALM::palm.get.summary()`; defaults to `"NULL"`.}

\item{useCluster}{Logical; if `TRUE`, uses clustering in Step2.1. When
`clusterFile` is not provided, PLINK input falls back to FID from `.fam`.
Defaults to `FALSE`.}

\item{clusterFile}{Optional two-column cluster file. The first column is
sample IID and the second column is cluster ID. A header row with
`IID` and `cluster` is accepted but not required. When provided, this
enables clustering and overrides the PLINK FID fallback.}
}
\value{
Invisibly returns a character vector of written file paths.
}
\description{
Reads genotype data from PLINK or VCF input, applies a pre-fitted PALM null
model, and writes per-phenotype summary statistics (one file per phenotype).
VCF input is read directly.
}
